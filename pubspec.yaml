name: flutter_gradient_selector
description: Gradient selector project.
version: 0.1.1
repository: https://github.com/mynumeric-mobile/flutter_gradient_selector

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  flutter_hsvcolor_picker: ^1.5.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

